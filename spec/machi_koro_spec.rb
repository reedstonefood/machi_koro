require "spec_helper"

describe MachiKoro do
  it "has a version number" do
    expect(MachiKoro::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
